# Project Instructions

- Do not start a dev server, Vite preview, Tauri preview, or any other local dev-preview process for this project unless the user explicitly asks for it in the current turn.
- Prefer non-interactive verification such as `npm test` and `npm run build`.
